<div id="<%= dom_id warehouse %>">
  <p>
    <strong>Name:</strong>
    <%= warehouse.name %>
  </p>

  <% warehouse.items.each do |item| %>
    <p>
      <%= item.name %> | <%= link_to "View Item", item %> 
    </p>
  <% end %>

</div>
